#include <string>

#include "symbole.h"
#include "automate.h"

class E7 : public Etat {
    public:
        E7(string name) : Etat(name) { };
        virtual ~E7() { };

    private:
        /* data */
};