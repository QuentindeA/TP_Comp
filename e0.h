#include <string>

#include "symbole.h"
#include "automate.h"

class E0 : public Etat {
    public:
        E0(string name) : Etat(name) { };
        virtual ~E0() { };

    private:
        /* data */
};