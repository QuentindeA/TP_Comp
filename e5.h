#include <string>

#include "symbole.h"
#include "automate.h"

class E5 : public Etat {
    public:
        E5(string name) : Etat(name) { };
        virtual ~E5() { };

    private:
        /* data */
};