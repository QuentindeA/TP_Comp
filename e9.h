#include <string>

#include "symbole.h"
#include "automate.h"

class E9 : public Etat {
    public:
        E9(string name) : Etat(name) { };
        virtual ~E9() { };

    private:
        /* data */
};