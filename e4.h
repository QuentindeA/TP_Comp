#include <string>

#include "symbole.h"
#include "automate.h"

class E4 : public Etat {
    public:
        E4(string name) : Etat(name) { };
        virtual ~E4() { };

    private:
        /* data */
};