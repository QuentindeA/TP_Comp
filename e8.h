#include <string>

#include "symbole.h"
#include "automate.h"

class E8 : public Etat {
    public:
        E8(string name) : Etat(name) { };
        virtual ~E8() { };

    private:
        /* data */
};