#include <string>

#include "symbole.h"
#include "automate.h"

class E6 : public Etat {
    public:
        E6(string name) : Etat(name) { };
        virtual ~E6() { };

    private:
        /* data */
};