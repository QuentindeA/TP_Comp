#include <string>

#include "symbole.h"
#include "automate.h"

class E2 : public Etat {
    public:
        E2(string name) : Etat(name) { };
        virtual ~E2() { };

    private:
        /* data */
};